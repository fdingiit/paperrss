# E2E Report: FC-20260313-02

- Card ID: `FC-20260313-02`
- Test Mode: Simulated
- Executor: Codex
- Executed At: 2026-03-13

## 背景

本 E2E 报告验证 `-status` 新功能在“正常 / 异常 / 幂等”三种典型路径下的行为。

## 决策

### 用例清单

| Case ID | 类型 | 前置条件 | 模拟依赖 | 结论 |
| --- | --- | --- | --- | --- |
| E2E-001 | 主路径 | daemon 运行中，`/healthz` 可用 | stub health payload | PASS |
| E2E-002 | 失败重试/降级 | `/healthz` 不可达 | 网络异常 stub | PASS |
| E2E-003 | 幂等重复触发 | 连续发送 3 次 `-status` | 固定 health payload | PASS |

### 用例详情

#### E2E-001

- 输入：`@bot -status`
- 预期：回复轻量卡片，字段至少含 Overall/Daily/Weekly/Next Run。
- 实际：返回轻量摘要结构（与设计一致）。
- 证据（命令/日志/截图）：模拟响应 payload 检查（字段存在性校验）。
- 结论：PASS

#### E2E-002

- 输入：`@bot -status`（health endpoint timeout）
- 预期：返回降级文本（例如 `status unavailable`），不抛异常。
- 实际：返回降级文本，流程继续。
- 证据（命令/日志/截图）：异常注入后响应路径检查。
- 结论：PASS

#### E2E-003

- 输入：连续 3 次 `@bot -status`
- 预期：每次响应一致，不修改状态文件，不影响后续命令。
- 实际：三次响应一致；未产生状态副作用。
- 证据（命令/日志/截图）：重复调用输出一致性检查。
- 结论：PASS

## 约束

- 本报告为模拟 E2E，不包含真实 Slack API 调用。
- 真实上线前建议补一条真实命令通路集成测试。

## 示例

- 字段校验示例：
  - `overall_status`
  - `daily_status`
  - `weekly_status`
  - `next_daily_run`

## 验收

- E2E-001~003 均 PASS，满足 G4。
